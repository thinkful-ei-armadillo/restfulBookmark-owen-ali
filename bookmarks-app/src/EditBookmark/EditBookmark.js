import React, { Component } from "react";

export default class EditBookmarkForm extends Component {
  /* state for inputs etc... */
  render() {
    return (
      <section className="EditBookmarkForm">
        <h2>Edit Bookmark</h2>
        <form>{/* inputs etc... */}</form>
      </section>
    );
  }
}
